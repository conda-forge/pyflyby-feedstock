{% set name = "pyflyby" %}
{% set version = "1.10.5" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 7dc5f66638ea141255617814b229805911e1cbf522f645da1cf7f081e787f92d

build:
  number: 0
  entry_points:
    - py = pyflyby._py:py_main
    - py3 = pyflyby._py:py_main
  script: |
    export CXXFLAGS="${CXXFLAGS} -D_LIBCPP_DISABLE_AVAILABILITY"  # [osx]
    export SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
    {{ PYTHON }} -m pip install . -vv
  skip: true  # [win]

requirements:
  build:
    - python
    - pybind11
    - meson-python
    - ninja
    - setuptools_scm
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
  host:
    - pip
    - python
    - setuptools
    - python
    - meson-python
    - pybind11
    - numpy
    - pip
  run:
    - pyflakes
    - six
    - toml
    - python
    - isort
    - black
    - prompt_toolkit
    - typing_extensions >=4.6  # [py<312]

test:
  imports:
    - pyflyby
  commands:
    - pip check
    - py --help
  requires:
    - pip
    - python

about:
  home: https://pypi.org/project/pyflyby/
  summary: pyflyby - Python development productivity tools, in particular automatic import management
  license: MIT
  license_file: doc/LICENSE.txt

extra:
  recipe-maintainers:
    - asmeurer
    - carreau
    - quarl
    - peytondmurray
    - krassowski
